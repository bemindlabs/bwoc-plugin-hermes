[project]
name = "bwoc-plugin-hermes"
version = "0.1.0"
description = "BWOC fleet adapter for Hermes."
license = { text = "MIT" }
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
