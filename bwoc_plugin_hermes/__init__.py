"""BWOC -> Hermes plugin. Registers tools that shell out to the `bwoc` CLI.

WIP scaffold — see README.
"""


def register(ctx):
    # TODO: ctx.register_tool(...) wrapping `bwoc list/send/run/...`,
    #       ctx.register_skill(...), and a MemoryProvider bridging `bwoc memory`.
    _ = ctx
